<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.interfdaces_usuario">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Interfdaces_Usuario">
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.Interfdaces_Usuario.NoActionBar"></activity>
        <activity
            android:name=".InterestSelector"
            android:label="@string/title_activity_interest_selector"
            android:theme="@style/Theme.Interfdaces_Usuario.NoActionBar" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".Conversacion" />
        <activity android:name=".PerfilVista" />
        <activity android:name=".menu" />
        <activity android:name=".Gustos" />
        <activity android:name=".Principal" />
        <activity android:name=".Registro" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Interfdaces_Usuario.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>